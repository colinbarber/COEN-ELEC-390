<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorBlack</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="NavBar" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:textColorPrimary">@color/colorWhite</item>
        <item name="android:fontFamily">@font/teko_medium</item>
        <item name="actionBarSize">20dp</item>
        <item name="android:textColorSecondary">@color/colorWhite</item>
    </style>

    <style name="AppTheme.PopupOverlaynner" parent="ThemeOverlay.AppCompat.Light">/
        <item name="android:colorBackground">@color/colorGrey</item>
        <item name="android:textColor">@color/colorWhite</item>
    </style>


</resources>
